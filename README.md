# pizza-order-system

A pizza ordering backend system built with **Go** and **MongoDB**, designed to demonstrate core **CRUD** (create, read, update, delete) operations and **RESTful** API principles using real **HTTP** request handling
